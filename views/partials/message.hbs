
{{#if success_msg}}
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        {{!-- 注意此處用3個花括號，這是可以解譯html tag的型態 --}}
        {{{success_msg}}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
{{/if}}

{{#if warning_msg}}
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        {{!-- 注意此處用3個花括號，這是可以解譯html tag的型態 --}}
        {{{warning_msg}}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
{{/if}}

{{#if errors}}
    {{#each errors}}
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            {{!-- 注意此處用3個花括號，這是可以解譯html tag的型態 --}}
            {{{ this.message }}}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {{/each}}
{{/if}}

